PORT=5000
FLASK_ENV=development

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dsairsofteam

# CORS (comma-separated). If not set, defaults allow localhost + deployed frontends.
# Example:
# CORS_ORIGINS=https://dsairsofteam.onrender.com,https://dsairsofteam.vercel.app

# Optional DB pool tuning (seconds)
DB_POOL_RECYCLE=300

# JWT
JWT_SECRET=tu_super_secret_key_aqui_cambiar_en_produccion
JWT_EXPIRE=604800
